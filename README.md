# ansible-easy

A Python package to simplify Ansible workflows — less boilerplate, more doing.

## Installation
```bash
pip install ansible-easy
# or with uv
uv pip install -e .
```


## Roadmap

- [ ] Core Ansible workflow simplification
- [ ] UI (coming soon)
